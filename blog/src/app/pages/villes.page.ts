import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'blog-romain-villes',
  standalone: true,
  imports: [RouterOutlet],
  template: `
    Ville work
  `,
})
export default class VillesComponent {}
